int findDuplicate(vector<int>& nums) {
        int n = nums.size();
        for( int i=0 ;i< n; i++ ){
            int ind = nums[i]%n;
            nums[ind] += n;
        }
        for( int i=0 ;i< n; i++ ){
            if( nums[i]/n >= 2 ){
                return i;
            }
        }
        return (-1);
    }
